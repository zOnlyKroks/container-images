base_image: "registry.access.redhat.com/ubi9/ubi-micro:latest"
version: "RELEASE.2024-10-13T13-34-11Z"
description: "MinIO High-Performance Object Storage - Downloads pre-compiled binaries from dl.min.io"

# Default platforms (can be overridden with --platform flag)
# Use: linux/amd64,linux/arm64 for multi-platform
# Or single platform like linux/arm64 for Apple Silicon
platforms: "linux/amd64,linux/arm64"

build_args:
  RELEASE: "RELEASE.2024-10-13T13-34-11Z"
